<div class="container">
    <a class="btn btn-danger mt-5" href="index.php">Voltar</a>
<div id="accordion" class="w-100 mt-5 border border-primary rounded">
    <div class="card mw-100" id="{{id}}">
        <form method="post">
        <div class="card-header mw-100" id="headingOne">
            <div class="mb-0 mw-100 d-flex  justify-content-between align-items-center">

                <input type="text" id="title" class="bg-light" action="edit.php" name="title"  value="{{title}}" style="border: none; outline: none; width: 25%;" >
                <div>

                    <input type="submit" value="Salvar" class="btn btn-primary {{hidden}} save2 {{save}}">
                    <a class="btn btn-danger"  href="{{url}}&action=delete" id="view" aria-expanded="true" aria-controls="{{id}}">
                        Excluir
                    </a>
                </div>
            </div>
        </div>

        <div id="collapseOne{{i}}" class="collapse show mw-100" aria-labelledby="{{id}}" data-parent="#accordion">
            <div class="card-body">
                <div class=" w-100 ">
                    <textarea class="form-control" id="textarea" name="note" style="border: none; resize: none; outline: 0" rows="10">{{note}}</textarea>
                </div>

            </div>
        </div>
        </form>
    </div>
</div>
</div>
